# Google Cloud Project Configuration
# For local development, the default value 'local-dev-project' will be used
# For production, set this to your actual GCP project ID
PROJECT_ID=local-dev-project

# Firestore Emulator Configuration
# Set to true to use local Firestore emulator (recommended for development)
# Set to false to connect to actual Google Cloud Firestore
USE_FIRESTORE_EMULATOR=true

# Identity Platform / Firebase Authentication
# Set to false for local development without authentication
# Set to true in production or when testing with real authentication
IDENTITY_PLATFORM_AUTH_ENABLED=false

# Optional: Override default authentication headers
# IDENTITY_PLATFORM_AUTH_PRIMARY_HEADER=Authorization
# IDENTITY_PLATFORM_AUTH_FALLBACK_HEADER=Authorization

# Application Configuration
QUARKUS_PROFILE=local # or "prod" for production

# MongoDB Configuration
# For local development with docker-compose
MONGODB_CONNECTION_STRING=mongodb://localhost:27017
MONGODB_DATABASE=tenant_db
# For production with authentication:
# MONGODB_CONNECTION_STRING=mongodb://username:password@mongodb-service:27017/tenant_db?authSource=admin

