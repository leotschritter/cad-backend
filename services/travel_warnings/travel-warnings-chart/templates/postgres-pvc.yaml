apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.postgresPvc.name }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.postgresPvc.storage }}
