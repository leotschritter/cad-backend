apiVersion: v1
kind: Secret
metadata:
  name: {{ include "tenant-service.fullname" . }}-smtp-secret
  labels:
    {{- include "tenant-service.labels" . | nindent 4 }}
type: Opaque
data:
  smtp_user: {{ .Values.smtp.user | quote }}
  smtp_password: {{ .Values.smtp.password | quote }}

