@startuml SystemContext
!include <C4/C4_Context>

!pragma layout smetana
LAYOUT_WITH_LEGEND()

' Persons
Person(traveller, "Traveller", "Signed-in user who plans trips, interacts socially, and receives safety notifications")

' Systems
System(travelSaaS, "Travel SaaS Platform", "Social travel management platform with itinerary planning, social interactions, and personalized recommendations")

' External Systems
System_Ext(identity, "Identity Provider", "OAuth2 / Firebase - User authentication and token validation")
System_Ext(auswaertigesAmt, "Auswärtiges Amt", "REST API providing official travel warnings and country safety information")
System_Ext(emailApi, "SendGrid Email API", "RESTful Email Delivery Platform used to send safety alerts and notifications")
System_Ext(weatherApi, "Meteosource Weather API", "REST API providing weather forecasts and meteorological data for travel destinations")

' Relationships
Rel(traveller, travelSaaS, "Plans trips, receives alerts, browses personalised feed", "HTTPS (REST API)")
Rel(travelSaaS, identity, "Authenticates users, validates identity", "OIDC / JWT")
Rel(travelSaaS, auswaertigesAmt, "Fetches travel warnings and safety data", "HTTPS (REST API)")
Rel(travelSaaS, emailApi, "Sends safety alerts and travel warning notifications via REST API", "HTTPS (SendGrid v3 API)")
Rel(travelSaaS, weatherApi, "Fetches weather forecasts for destinations", "HTTPS (REST API)")

@enduml
