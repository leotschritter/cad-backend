# Helm values for itinerary-service in freemium namespace (DEV)
namespaceOverride: freemium

itineraryService:
  image: "ghcr.io/leotschritter/cad-backend:latest"

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations:
    iam.gke.io/gcp-service-account: "travel-backend-sa@iaas-476910.iam.gserviceaccount.com"
  name: "itinerary-service-sa-freemium"

gcp:
  projectId: "iaas-476910"
  serviceAccountEmail: "travel-backend-sa@iaas-476910.iam.gserviceaccount.com"
  bucketName: "iaas-476910-tripico-images"

backendUrl: "https://itinerary-freemium.dev.tripico.fun"

podAnnotations:
  tier: freemium
  environment: dev

service:
  type: ClusterIP
  port: 8080
  targetPort: 8080
  name: http

ingress:
  enabled: true
  className: "nginx"
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
  # Host will be set via --set ingress.host in pipeline
  host: ""
  # TLS secret name will be set via --set ingress.tlsSecretName in pipeline
  tlsSecretName: ""

resources:
  limits:
    cpu: 500m  # Lower for dev
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 256Mi

autoscaling:
  enabled: false  # Disabled for dev
  minReplicas: 1
  maxReplicas: 2
  targetCPUUtilizationPercentage: 70

# Quarkus application configuration
quarkus:
  http:
    port: 8080
    host: "0.0.0.0"
  profile: "dev"

# Database configuration - separate DB for freemium dev
database:
  user: "cad_db_user"
  password: "" # provided via --set
  url: "" # provided via --set - should point to freemium dev DB instance

# Identity Platform Authentication Configuration
identityPlatform:
  authEnabled: "true"  # Disabled for easier dev testing
  primaryHeader: "X-Forwarded-Authorization"
  fallbackHeader: "Authorization"

# Service discovery for shared services
sharedServices:
  weatherService:
    url: "http://weather-forecast-service.shared.svc.cluster.local:8080"
  travelWarningsService:
    url: "http://travel-warnings-service.shared.svc.cluster.local:8080"

# Health check probes
probes:
  startup:
    httpGet:
      path: /q/health/started
      port: 8080
    initialDelaySeconds: 10
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 30
  liveness:
    httpGet:
      path: /q/health/live
      port: 8080
    initialDelaySeconds: 60
    periodSeconds: 30
    timeoutSeconds: 5
  readiness:
    httpGet:
      path: /q/health/ready
      port: 8080
    initialDelaySeconds: 30
    periodSeconds: 10
    timeoutSeconds: 5
